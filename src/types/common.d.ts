declare type Color = "red" | "yellow" | "blue" | "white" | "purple" | "grey" | "pink";

declare type ColorState = "nil" | "off" | "seed" | "on";

declare type Info = {
	appVersion: string;
	seedsVersion: string;
};